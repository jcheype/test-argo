apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-jc-ingress
  namespace: testjc
spec:
  rules:
    - host: test.lab.jcheype.com
      http:
        paths:
          - backend:
              service:
                name: test-jc
                port:
                  number: 80
            path: /
            pathType: Prefix
